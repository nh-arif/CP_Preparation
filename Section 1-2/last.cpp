#include <iostream>
using namespace std;

#define ll long long int

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    double start = clock();

    cerr << "Run Time : " << ((double(clock() - start)) / CLOCKS_PER_SEC);

    return 0;
}